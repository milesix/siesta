SIESTA can be built:

* Using CMake (see file INSTALL.CMake for details)
  This is now the only supported way.

  There are still some makefiles in the distribution, but
  they should not be used.
  






 