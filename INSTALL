SIESTA can be built using CMake (see file INSTALL.CMake.md for details)

This is now the ONLY supported way.

Makefiles and .mk files used in the legacy build scheme are still around, but
are not being updated.







 